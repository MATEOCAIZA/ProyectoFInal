const ObservationRepository = require("../src/repositories/ObservationRepository");

describe("ObservationRepository", () => {
  it("debería estar definido", () => {
    expect(ObservationRepository).toBeDefined();
  });
});
