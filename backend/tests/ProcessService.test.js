const ProcessService = require("../src/services/ProcessService");

describe("ProcessService", () => {
  it("debería estar definido", () => {
    expect(ProcessService).toBeDefined();
  });
  // Agrega aquí tests reales para las funciones que sí existen en ProcessService.js
});
