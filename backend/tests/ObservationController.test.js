const {
  ObservationController,
} = require("../src/controllers/ObservationController");

describe("ObservationController", () => {
  it("debería tener métodos definidos", () => {
    expect(ObservationController).toBeDefined();
  });
});
