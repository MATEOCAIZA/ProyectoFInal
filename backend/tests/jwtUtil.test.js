const jwtUtil = require("../src/utils/jwtUtil");

describe("jwtUtil", () => {
  it("debería estar definido", () => {
    expect(jwtUtil).toBeDefined();
  });
  // Agrega aquí tests reales para las funciones que sí existen en jwtUtil.js
});
